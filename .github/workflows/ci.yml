name: test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Test Windows GITHUB_WORKSPACE
        run: |
          echo $GITHUB_WORKSPACE
